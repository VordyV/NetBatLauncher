[project]
name = "server"
version = "0.1.0"
description = ""
authors = [
    {name = "Vordy"}
]
requires-python = ">=3.10,<4"
dependencies = [
    "fastapi (>=0.141.1,<0.142.0)",
    "uvicorn (>=0.52.4,<0.53.0)",
    "tortoise-orm (>=1.1.8,<2.0.0)",
    "tzdata (>=2026.3,<2027.0)",
    "pyyaml (>=6.0.3,<7.0.0)",
    "pydantic (>=2.13.5,<3.0.0)",
    "authx (>=1.7.1,<2.0.0)",
    "apscheduler (>=3.11.3,<4.0.0)",
    "python-dotenv (>=1.2.3,<2.0.0)",
    "flet[web] (>=1.0.0,<2.0.0)",
    "flet-datatable2 (>=1.0.0,<2.0.0)",
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
